# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration (Leave empty to use local JSON database fallback automatically)
MONGODB_URI=

# CORS Configuration
CLIENT_URL=http://localhost:5173
