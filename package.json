{
  "name": "modhera-sunrise-resort-root",
  "version": "1.0.0",
  "description": "MERN Stack website for Modhera Sunrise Resort",
  "main": "server.js",
  "scripts": {
    "install-all": "npm install && npm install --prefix backend --legacy-peer-deps && npm install --prefix frontend --legacy-peer-deps",
    "dev": "concurrently \"npm run start --prefix backend\" \"npm run dev --prefix frontend\"",
    "backend": "npm run start --prefix backend",
    "frontend": "npm run dev --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "mongodb": "^7.3.0"
  }
}
